// Alarm[0] event for obj_example
if (global.timetraveleffectsshown=true) {
    animation_frame = 1 - animation_frame; // Toggle between 0 and 1
    alarm[0] = 30; // Reset the alarm for the next frame switch (adjust as needed)
}
