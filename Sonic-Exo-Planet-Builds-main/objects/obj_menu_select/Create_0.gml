state = PAUSE_STATE_NAVIGATION; // Set initial state
highlight_timer = 0;            // Initialize highlight timer
option_id = 0;                  // Start at the first menu option

// Initialize the c_framework object
c_framework.state = PAUSE_STATE_NAVIGATION;
